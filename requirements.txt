# --- Standard Python Libraries ---
# (Tkinter und winsound sind in der Standardbibliothek enthalten, müssen also NICHT installiert werden)

# --- Entwicklungs- und Qualitäts-Tools ---
flake8==7.1.0             # Codeanalyse (PEP8-Prüfung)
mypy==1.10.0              # Statische Typprüfung
pytest==8.3.2             # Test-Framework
pytest-cov==5.0.0         # Testabdeckung

# --- Dokumentation ---
mkdocs==1.5.3             # Optionale Tooling für Projektdoku
mkdocs-material==9.5.10   # Schönes Theme für MkDocs

# --- Optionale Extras ---
playsound==1.3.0          # (Alternative zu winsound für Mac/Linux)
